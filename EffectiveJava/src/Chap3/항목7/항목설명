항목7:equals 메소드를 재정의할때 표준 구현 계약을 지켜라.
	재정의 하지 않는 경우.
1.각 인스턴스가 원래부터 유일한 경우. 인스턴스가 값이 아닌 Thread와 같이 행동하는 것, 단순객체가 아니면 기존 equals를 사용해야한다.
2.논리적인 동등성 검사가 의미가 없는경우 기존 equals를 사용한다.->랜덤 함수와 같은 것에서 사용한다면 실제로 거의 필요없음.
3.상위클래스에서 적절히 equals를 재정의하였고 이걸 가져와 사용하면 되는 경우.
4.private이거나 package-private 클래스여서 equals가 호출 안되는 경우.

equals 메소드는 동등 관게를 구현한다.
반사적이다. 자기자신과 equals하면 true이다.
대칭적이다. x->y ==true이면 y->x ==true이다.
추이적이다. x->y ==true y->z ==true이면 x->z==true이다.
일관적이다. 정보가 바뀌지 않는다면 결과는 일관적이어야함.
null이 아닌 모든 참조에서 x.equals(null)은 무조건 false이다.