항목32:적절한 타입 대신 문자열 쓰지 마라.
문자열이 text는 몰라도 숫자나 boolean값일때는 그에 적절한 값을 하도록하는것이 좋다. 
만약 문자+구분자+숫자의 경우 구분자가 '#'이라고 가정을 하자, 이러한 #이 문자에서 입력이 된다면
큰 문제가 생길것이다. 그리고 문자열을 특정 기능으로 사용하지마라, 대표적으로  쓰레드 에서 특정 함수를 
문자열로 저장하는 함수를 만든다면, 이러한 함수는 모두가 접근이 가능해서 문제가 난다.